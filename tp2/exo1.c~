#include <omp.h>
#include <stdio.h>

int main(int argc, char** argv) {
  int n = 3;
  int i;
  for (i = 0; i < argc; i++) {
    printf("Liste argv %ld : %s \n", i+1, argv[i]);
    printf("coucou");
  }
  getchar();
  return 0;
}

